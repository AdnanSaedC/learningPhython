# object types/ data types

# Number - Decimal() ,Fraction()

# List is nothing but array - [1,2.5,"sttst"] here bracket

# tuple array with paranthesis() just immutable list

# dictionary - object{}

# set it is nothing but it keeps only unique values

# files
# None - empty(no value at all)
# boolean


# string 
# len(stringName)
# string[0] you can get the character but u cant update the char because string is immutable
# string[0:4] slicing - starts from 0 to 3 index

# dir(stringName) - it will give all the functions available for string
# dirName(key)

# >>> repr('x') this is the actual repr high precison
# "'x'"
# >>> str('x') this is what visible to the user less precison
# 'x'
# >>> print('x') this is for screens least prscision dsigned fro screen but the above are for programmes
# x